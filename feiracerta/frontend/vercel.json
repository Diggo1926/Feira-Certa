{
  "buildCommand": "echo \"window.ENV_API_URL = '$API_URL'; window.ENV_APP_SECRET_TOKEN = '$APP_SECRET_TOKEN';\" > env.js",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
